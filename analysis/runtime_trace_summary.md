# PRISM-TopoMap 运行追踪摘要

## 输入与解析范围

- 日志：`/home/tom/host_catkin_ws/src/PRISM-TopoMap/data_flow.log`
- 文件大小：2,248,429 字节
- 物理行数：8,990
- 含 `[FLOW]` 的物理行数：7,318；解析片段数：7,422（交错行可能含两个片段）
- 成功帧点云时间范围：1517156141.444385767–1517156230.939493656，跨度 89.495108 s
- `STAGE`：CLOUD_PARSE, DECISION, DESCRIPTOR, DESCRIPTOR_PY, EDGE, FAISS, GRID, LOCALIZATION_RESULT, LOCALIZER_SNAPSHOT, ODOM, PUBLISH, REGISTRATION, REGISTRATION_PY, REGISTRATION_SERVICE, RX, SUMMARY, SYNC, THROTTLE, VERTEX
- 日志时间字段：首条 logger wall time `1785290356.144806558`（L1），末条 `1785290445.678336390`（L8989）；物理首/末 logger 的 ROS time 分别为 `1517156141.482713852` 和 `1517156231.021635375`。
- 检测到 8852 个显式 ROS/sim-time 第二时间戳，其中非零 8842 个；全文非零范围为 `1517156141.452424307`–`1517156231.021635375`，ROS time 已正常推进。

## 消息与拓扑统计

- 总点云消息数：896（`rx_id` 从 1 到 896；其中 3 条 RX 行被交错输出截断，缺失 ID 为 [109, 248, 561]）。
- 被 0.3 s 间隔限流：654（结构化行直接解析出 651 条，另 3 条由连续 `rx_id` 总数减成功帧数还原）。
- 同步等待或失败记录：1（L21）。
- 成功处理帧：242（以 `SYNC result=OK` 计；SUMMARY/PUBLISH 被交错截断时仍保留该帧）。
- 节点数量：0 → 9；可完整解析的 `VERTEX CREATE` 8 条，另有 1 条创建记录被交错截断。
- 最终边数：8；新增边事件 8 次，边添加拒绝 0 次。
- 回环事件：0。
- `decision` 次数：`FIRST_VERTEX` 1，`KEEP` 233，`NEW_VERTEX` 8。

这里的“边添加拒绝”只统计 `STAGE=EDGE` 的添加结果；`EDGE_REATTACH ... result=REJECT` 是节点切换尝试失败，不是图中边被拒绝。

## 典型耗时

- C++ 描述符服务调用：n=242，中位数 58.743 ms，P10–P90 26.135–117.688 ms，范围 0.759–215.387 ms。
- Python 描述符前向计算：n=239，中位数 6.227 ms，P10–P90 5.508–7.632 ms，范围 5.376–83.152 ms。
- Python 描述符服务总耗时：n=239，中位数 53.994 ms，P10–P90 23.183–111.967 ms，范围 22.257–210.070 ms。
- LocalGrid 总更新：n=237，中位数 4.179 ms，P10–P90 4.030–4.752 ms，范围 3.852–11.030 ms。
- occupancy 更新：n=237，中位数 4.175 ms，P10–P90 4.028–4.751 ms，范围 3.851–11.027 ms。
- FAISS：n=177，中位数 0.010 ms，P10–P90 0.000–0.031 ms，范围 0.000–0.063 ms。
- 配准：n=435，中位数 7.853 ms，P10–P90 6.946–13.617 ms，范围 0.810–107.867 ms。

## 定位结果延迟与跨帧关系

- 定位结果消费延迟：n=156，中位数 399.926 ms，P10–P90 300.418–700.462 ms，范围 300.107–800.104 ms。
- 快照写入 239 次；归一化 Timer 周期 179 次，其中 FAISS 空索引 61 次、非空候选 118 次。
- 候选配准调用/结果各 466 次；其中明确 `MATCHED` 25 次。LocalizedState 结果生成 119 次。
- 主循环消费记录 238 次；其中 155 次 matched/unmatched 至少一项非零。
- 新旧格式同时出现时已去重：旧式记录补齐 26 个被交错截断的结构化事件；其余旧式记录仅作为旁证，不重复计为第二次调用。
- 真实跨帧链：FRAME 162 在 L5373 写入快照，L5394 被 Timer 读取，L5399 得到非空 FAISS 候选，5 个候选完成配准（匹配 1 个），L5411 写回结果；FRAME 163 在 L5420 消费该 `result_stamp`。

## 数据质量说明

- `36` 个 `[FLOW]` 片段因混合进程输出交错而未解析出完整 `STAGE`；解析器保留其原始行号作为警告，不据此补造事件。
- `0` 条 Python 描述符记录无法按 `STAMP` 关联到成功帧。
- 首帧描述符服务临时失败（`success=false`、维度 0），但栅格和首节点仍被真实创建；其余 241 帧服务结果均为成功的 256 维描述符。这是本次运行现象，不是算法固定行为。

## 推荐案例

- 案例 A：FRAME 1，首节点创建。
- 案例 B：FRAME 162→163，真实展示快照写入、Timer 定位、非空 FAISS、候选配准、结果写回、后续帧消费和 KEEP。
- 案例 C：FRAME 106，距离 5.3755 m 超过 5.0000 m，创建 vertex 2 并添加顺序边 1→2。
- 本次未观察到 `EDGE_SWITCH`、`LOCALIZATION_SWITCH` 或 `LOOP_CLOSURE`；不选择、不虚构对应案例。
